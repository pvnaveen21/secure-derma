services:
  - type: web
    name: secure-derma-backend
    env: python
    rootDir: back-end
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
