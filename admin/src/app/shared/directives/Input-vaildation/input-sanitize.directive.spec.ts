import { InputSanitizeDirective } from './input-sanitize.directive';

describe('InputSanitizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSanitizeDirective();
    expect(directive).toBeTruthy();
  });
});
